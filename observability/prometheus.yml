global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "tmatch-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]
  - job_name: "tmatch-ml"
    metrics_path: /metrics
    static_configs:
      - targets: ["ml:8001"]
